[package]
name = "matrix-guard"
version = "0.1.0"
edition = "2021"
description = "Matrix Linux sandbox spawn and supervision budget"
authors = ["enrell"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/enrell/matrix"
readme = "README.md"
[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
