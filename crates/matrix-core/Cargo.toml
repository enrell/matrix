[package]
publish = false
name = "matrix-core"
version = "0.1.0"
edition = "2021"
description = "Matrix kernel core: contexts, resources, dependencies, tickets and local lifecycle"
authors = ["enrell"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/enrell/matrix"
readme = "README.md"

[dependencies]
serde_json = "1"
parking_lot = "0.12"
