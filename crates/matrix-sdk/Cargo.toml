[package]
publish = false
name = "matrix-sdk"
version = "0.1.0"
edition = "2021"
description = "Matrix legacy generic daemon client (frozen compatibility surface)"
authors = ["enrell"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/enrell/matrix"
readme = "README.md"

[dependencies]
serde_json = "1"
