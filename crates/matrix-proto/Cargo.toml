[package]
publish = false
name = "matrix-proto"
version = "0.1.0"
edition = "2021"
description = "Matrix executable wire spec: framing, envelope, handshake, errors and messages"
authors = ["enrell"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/enrell/matrix"
readme = "README.md"

[dependencies]
serde_json = "1"
