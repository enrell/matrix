[package]
publish = false
name = "matrix-host"
version = "0.1.0"
edition = "2021"
description = "Matrix local process host: spawn, sessions, dispatch and supervision"
authors = ["enrell"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/enrell/matrix"
readme = "README.md"

[dependencies]
matrix-guard = { path = "../matrix-guard" }
matrix-core = { path = "../matrix-core" }
matrix-proto = { path = "../matrix-proto" }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
matrix-component = { path = "../matrix-component" }
